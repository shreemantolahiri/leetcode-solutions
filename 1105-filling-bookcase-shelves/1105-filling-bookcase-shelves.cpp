class Solution {
public:
    int solve(int ind,int row, int col,vector<vector<int>>& books, int shelfWidth){
        if(ind==books.size()) return 0;

        int currWidth=books[ind][0];
        int currHeight=books[ind][1];
    }
    int minHeightShelves(vector<vector<int>>& books, int shelfWidth) {
        
    }
};